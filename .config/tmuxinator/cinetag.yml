# /Users/yuta/.config/tmuxinator/cinetag.yml

name: cinetag
root: ~/

windows:
  - editor:
      layout: even-horizontal
      panes:
<% `git -C ~/Develop/cinetag worktree list --porcelain`.scan(/worktree (.+)/).flatten.each do |path| -%>
        - cd <%= path %> && clear
<% end -%>
